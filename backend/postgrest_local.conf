# PostgREST — configuración LOCAL (desarrollo)
# Para producción usar: postgrest.conf

db-uri = "postgres://postgres:posgres@localhost:5432/specialcoffee"
db-schema = "public"
db-anon-role = "anon"
server-port = 3001
server-host = "127.0.0.1"

# Debe coincidir con JWT_SECRET en backend/auth/.env
jwt-secret = "specialcoffee_dev_local_secret_2025_ok"

db-schema-cache-ttl = 0
db-pool = 5
log-level = "info"

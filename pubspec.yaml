name: special_coffee
description: SpecialCoffee AI — Q Grader digital para producción y preparación de café de especialidad.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

  # State management
  flutter_riverpod: ^3.0.0
  riverpod_annotation: ^4.0.0

  # Navigation
  go_router: ^14.6.1

  # Immutable models & serialization
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0

  # Local DB (offline-first)
  drift: ^2.21.0
  sqlite3_flutter_libs: ^0.5.24
  path_provider: ^2.1.5

  # Networking — PostgREST + Auth service
  dio: ^5.7.0
  flutter_secure_storage: ^9.2.2
  jwt_decoder: ^2.0.1
  connectivity_plus: ^6.1.0

  # Gemini AI
  google_generative_ai: ^0.4.7

  # Utils
  uuid: ^4.5.1
  intl: ^0.19.0
  logger: ^2.5.0
  equatable: ^2.0.5

  # Notifications
  flutter_local_notifications: ^18.0.1

  # UI helpers
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  fl_chart: ^0.69.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mocktail: ^1.0.4

  # Code generation
  build_runner: ^2.15.0
  freezed: ^3.0.0
  riverpod_generator: ^4.0.0
  json_serializable: ^6.8.0
  drift_dev: ^2.21.0

  # Linting -- deshabilitado: custom_lint 0.8.x (analyzer ^8) y riverpod_lint 3.1.1+
  # (analyzer ^9) son mutuamente incompatibles. Habilitar cuando el ecosistema converja.
  # custom_lint: ^0.8.1
  # riverpod_lint: ^3.1.0


flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/rules/

  # fonts: (uncomment after adding font files to assets/fonts/)
  #   - family: DMSerifDisplay
  #     fonts:
  #       - asset: assets/fonts/DMSerifDisplay-Regular.ttf
  #       - asset: assets/fonts/DMSerifDisplay-Italic.ttf
  #         style: italic
  #   - family: Inter
  #     fonts:
  #       - asset: assets/fonts/Inter-Regular.ttf
  #       - asset: assets/fonts/Inter-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/Inter-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/Inter-Bold.ttf
  #         weight: 700
  #   - family: JetBrainsMono
  #     fonts:
  #       - asset: assets/fonts/JetBrainsMono-Regular.ttf
  #       - asset: assets/fonts/JetBrainsMono-Medium.ttf
  #         weight: 500
